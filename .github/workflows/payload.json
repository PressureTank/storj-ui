{
  "text": "GitHub Action build result for ${{ github.workflow }}: ${{ job.status }}\n<$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID|Run Results>",
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "GitHub Action build result for ${{ github.workflow }}: ${{ job.status }}\n<$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID|Run Results>"
      }
    },
    {
      "type": "divider"
    },
    {
      "type": "actions",
      "elements": [
        {
          "type": "button",
          "text": {
            "type": "plain_text",
            "text": "${{ github.workflow }}",
            "emoji": true
          },
          "url": "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
        },
        {
          "type": "button",
          "text": {
            "type": "plain_text",
            "text": "Trace Viewer",
            "emoji": true
          },
          "url": "https://trace.playwright.dev/"
        }
      ]
    }
  ]
}
